pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'chmod +x test.sh '
                sh './test.sh'
            }
        }
    }
}

